apply plugin: 'war'

version = '1.0'

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    compile 'org.jodd:jodd-joy:5.0.0'
}

task wrapper(type: Wrapper) {
	gradleVersion = '4.4'
}